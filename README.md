Para poder usar la base de datos: 
1.En MySQL Workbench, en el panel Navegador, seleccione "Server"
2. De las opciones que aparecen seleccionar "Data import"
3. Seleccione la carpeta de proyecto o el archivo SQL independiente, elija el esquema en el que se importarán los datos o seleccione el botón Nuevo para definir un nuevo esquema.
4. Seleccione "Star Import" y selecionamos nuestra base "repositorios.sql"para comenzar el proceso de importación.

IMPORTANTE
posterior a la instalacion de la base de datos busca en tu dispositivo el siguiente archivo:

C:\xampp\php\php.ini

dentro dek archivo deberas buscar la extencion "gd" y para habilitarlo se elimina el punto y como
que se encuentran antes y finalizamos con el aguardado del archivo. Cabe mencionar que la extension debera quedar de la siguiente versión:
extension: gd
